[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[dev]
  framework = "#static"
  
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[template.environment]
  NETLIFY_BLOB_LIMIT_MB = "100"