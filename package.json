{
  "name": "nugt",
  "version": "3.7.1",
  "description": "NUFC GameTime App",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "netlify dev",
    "setup-team": "node setup-team.js",
    "validate-config": "node validate-config.js",
    "test-config": "node validate-config.js && echo 'Opening test page...' && start test-config.html"
  },
  "dependencies": {},
  "devDependencies": {
    "netlify-cli": "^15.0.0"
  }
}
